# Inkscape script automation cards

A script in nodeJS for automation  of banners creation.

## How to use

Install npm and node then run:
```bash
npm i
```

Automate with command:
```bash
npm run start model.svg ./photos/
```